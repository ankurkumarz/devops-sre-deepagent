"""Tools for Infrastructure Agent - Kubernetes MCP operations."""

# TODO: Implement Kubernetes MCP tools
# These will be integrated with LangGraph's MCP support

# Example tool structure:
# - get_pod_status(namespace: str, pod_name: str = None)
# - get_pod_logs(namespace: str, pod_name: str, container: str = None)
# - get_events(namespace: str, resource_type: str = None)
# - get_resource_metrics(namespace: str, resource_name: str)
# - get_service_status(namespace: str, service_name: str = None)
# - get_deployment_status(namespace: str, deployment_name: str = None)

